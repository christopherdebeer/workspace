# Documentation & Implementation Gaps

> **Doc-set provenance.** Generated by the `substrate-platform-docs` workflow
> (run `wf_b7b1386f-629`): 70 agents, 68 succeeded. Two agents failed on transient
> API errors — `inv:services-cells` (server error mid-response) and
> `doc:runtime-cell-sdk` (empty output). Both were recovered afterward (the
> services-cells doc hand-written from source + the live `$cells` snapshot; the
> cell-sdk doc regenerated from its recovered inventory), so items 2–3 below are
> now **RESOLVED** and retained only for provenance. One `verify` sub-agent also
> failed (StructuredOutput cap), leaving one coherence finding kept unverified.

- ✅ **RESOLVED** — platform-derivatives.md shipped empty (56 bytes); the workflow's `derivativesMarkdown` deliverable now fills it. Confirmed written.
- ✅ **RESOLVED** — platform-reference/runtime-cell-sdk.md was an empty stub (the doc agent returned no markdown). Now written (~49KB) covering all 11 capabilities from its recovered inventory.
- ✅ **RESOLVED** — platform-reference/services-cells.md was missing (its inventory agent failed on an API error). Now written from source + the live `$cells` snapshot: registry, describeTypes, cellContracts, ssrReads/callerWrites, invokeCell/x-cell-caller, cell-template IAM, transpile, async deploy, S3 layout. It is a **distinct doc** (the Cell axis control plane), not folded into services-edge.
- DEAD CODE: platform/runtime/present.ts (resolvePresent/resolveLabel) has zero non-reexport, non-test callers in platform/ or services/ source; it ships only via the cell-sdk bundle. Decide: wire it in as the real per-fact present stage, or delete it and rewrite platform-core.md:190,291 + ADR-0012 which mis-cite it at service.ts:335-339 (that line is the unrelated CORE_FACT_VERBS set).
- STALE SYNC COPIES: cells/home-next/shared/ (federated-renderer.ts, ui.tsx, vocab.ts, form.tsx) and cells/home-next/shared.tsx are orphaned GENERATED files whose generator scripts/sync-platform-ui.mjs was deleted (they were a one-shot backup snapshot in commit 696d805, not a live artifact). Nothing imports them (home-next uses @parc/ui). Delete the directory.
- HOME/HOME-NEXT FORK: cells/home and cells/home-next carry byte-identical-then-drifting copies of the present/shaping/renderer-host logic. home-next is the ACTIVE security-hardened successor but is NOT the default cell (DISPATCH_DEFAULT_CELL still 'c15r/home' / 'deferred'). The migration is half-done; resolve it (collapse or promote to @parc/ui) so present/security fixes land once.
- SALIENCE SIGNAL COUNT DRIFT: architecture.md §4.2.X documents FIVE signals; compose.md/ADRs and the shipped state.ts implement SEVEN (relevance ADR-0051 + reward ADR-0070, both default weight 0). architecture.md should be updated to the 7-signal blend or explicitly note the two default-0-weight completions.
- STALE LINE ANCHORS in platform-core.md: state.ts has grown to ~2267 lines; cited line numbers are systematically stale (EntryMeta cited :49 is at :51; put cited :1206 is at :1645; appendTrajectory-in-put is at :1746). Every cited SYMBOL exists and behaves as described, but a maintainer following line numbers will land wrong. Re-anchor or switch to symbol references.
- GROUP WRITE GRANTS ARE NON-FUNCTIONAL (fail-closed): requireWriteThrough does not route through the shared applicableGrants resolver, so group-scoped and crossSlice write grants are silently denied though grants() advertises write as a working gate. A shipped, documented, read-visible feature authorizes nothing. Fix + add a regression test.
- UNBOUNDED $graph / edges() default: scopeEdges returns ALL edges when limit===undefined; $graph is gateway-reachable with no size guard (unlike $catalog's overBudget guard) and ADR-0069's replacement inherits the bug. ADR-0081 already records a live CloudFront 30s/6MB failure. Needs a default page bound before the corpus grows.
- PROPOSED-BUT-NOT-SHIPPED ADRs referenced as live: ADR-0016 (edges first-class) is Status: Proposed, only phase-1 rel-vs-label inspector ships; ADR-0088 Inc 1 (long-poll changes) is unshipped and is the cheapest single-origin external push. Mark clearly in docs which ADRs are aspirational vs shipped.
- DUAL CHANGE-PROPAGATION SUBSTRATES with no single origin: the DynamoDB stream (vector-indexer + archiver) and hand-emitted EventBridge workspace.fact.written (~10 sites, reactor) are selected inconsistently; state.put() does not own the emit. A new write path that forgets the emit silently breaks reactions. Decide the canonical origin (stream, or state.put-owned emit).
- VECTOR DIMENSION decided twice with divergent case-sensitivity: module-level DIM in planStreamWork uses strict 'bedrock' comparison; vectorsFromEnv lowercases. Latent (infra pins VECTOR_DIM='1024') but VECTOR_EMBEDDER=Bedrock (capital) would put 1024-dim vectors into a d256 index. Thread vectors.embedder.dimension as the single source.
- VECTOR INDEX-MEMBERSHIP predicate forked: stream indexer drops delete-timer facts (handler.ts:106), reindex worker does not gate on timerEffect, so a full reindex re-embeds LIVE delete-timer leases that leak into search/recall. No shared shouldIndex(fact). Admin-only trigger but should be one predicate.
- STATESTORE CODEC PARITY untested: v3 codec is a hand-maintained field allowlist, memory store is a structural spread; a new StateRecord field wired through put() but not the codec is dropped in prod while tests pass. Add a codec-parity round-trip test; also pin touch-counter rollover equivalence between the two backends.
- The coherence audit's live-snapshot corroboration (live/edges.json, live/recall.json, live/types.json, live/manifest.json, '29018 edges', '17 cells', 'public:true') is NOT verifiable from repo source — these are runtime artifacts, not tracked files. The repo has 15 cell directories. Where source-vs-deployed claims matter, capture a checked-in snapshot or clearly label them runtime observations.
- vocab.ts sync asymmetry (architecture.md §2.1.5.7 open question): the resolver is vendored to home but not starter/kernel; kernel uses its own inline hrefOf/titleOf fallback. Consolidate onto @parc/ui's vocab (the ADR-0044 one-source-no-drift intent) or document the accepted two coexisting routing surfaces.
- TTL table-config gap (architecture.md §7.x.4): cell-template.ts provisions per-cell tables WITHOUT a TimeToLiveSpecification, so a ttl attribute written by cell code is a no-op until enabled out-of-band. Either make the template enable TTL on the ttl attribute or document the operational consequence.
- selectorFromQuery not extracted: a query:{tags:[...]} view derives zero inView backbone edges (dropped at an earlier gate) while members() returns its members — View-as-edge ≠ View-as-query. Extract one selectorFromQuery(q) carrying all four Selector fields and share it.