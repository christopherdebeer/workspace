# Polish and measurement pass

Three source files changed: client/main.ts, client/audio.ts, client/flora-ez.ts. Exact replacements preserve intervening world/rig changes.

Visual: broad analytic crown interior shading in object space, plus modest grass root shading before the existing lighting and pixel postprocess. No geometry, texture download or draw-call additions. Flowers retain their separate head colour. No glow or transparency treatment added.

Audio: deterministic rain impact loop generated once at arm; separate outdoor rain wash and roof impacts, cab filtering/attenuation, enclosure shelter for roof impacts, wet-road tyre hiss, stereo positioning held through bird phrases, and gradual dawn activity. Existing mute/gesture/audio-session behaviour is retained. Rain adds two persistent source/filter/gain chains. New mix diagnostics expose rain/roof/cabin/enclosure.

Performance: redrape compares float32 heights and skips unchanged vertex/normal/bounds updates. Tree refresh may defer through an expensive frame for at most 180 ms, retaining the current population. This is bounded scheduling, not incremental population construction. Tree phase totals now distinguish gathering, admission, placement, uploads and shrubs.

Telemetry: previous frame closes before current tick begins using performance.now entry intervals. Slow threshold agrees with histogram at 50ms. Percentiles explicitly describe recent 4096 samples; tick session maximum is separate. Gap is unmeasured time, not a GPU inference. Tree batches count nonempty near/far meshes; casting counts only nonempty near meshes.

Validation: strict client TypeScript check passed; node client/polish-check.mjs passed (requires esbuild and three); real Three Lambert material hook expansion passed. Tests exercise frame accounting, exact 50ms, unchanged float32 redrape, welded vertices, deterministic rain, mute allocation, cab and tunnel effects. No GPU render or subjective listening verification: device visual/audio review and matching-route telemetry remain necessary. Do not infer a measured FPS gain from these component tests.
