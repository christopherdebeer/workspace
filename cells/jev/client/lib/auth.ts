/** Session + substrate client = the kernel's (sign in once, origin-wide; apex-aware). Reference, not copy. */
export { ensureAuth, isAuthed, mcp, login, signOut } from 'https://parc.land/@c15r/kernel/app.js';
