{
  "types": [
    {
      "type": "doc",
      "icon": "📄",
      "label": "value.title",
      "manager": "@c15r/lit",
      "note": "A document is a VIEW over cell-facts. Membership + order live in `_doc/<docId>/<blockKey>` decorations (type doc-order). The doc value holds only {id, title, summary}.",
      "schema": {
        "id": "string — the doc key, doc:<slug>",
        "title": "string",
        "summary": "string — one-line abstract"
      },
      "handlers": {
        "open": [
          {
            "path": "?doc=${match}"
          }
        ],
        "edit": [
          {
            "path": "?doc=${match}&edit=1"
          }
        ],
        "render": [
          {
            "hint": "markdown"
          }
        ]
      }
    },
    {
      "type": "doc-block",
      "icon": "🧱",
      "label": "value.id",
      "manager": "@c15r/lit",
      "note": "A document block: markdown + lit fences. Placed into a doc by a `_doc/` doc-order decoration.",
      "schema": {
        "id": "string — the block key",
        "content": "string — markdown with lit fences"
      },
      "handlers": {
        "render": [
          {
            "hint": "markdown"
          }
        ]
      }
    },
    {
      "type": "doc-order",
      "icon": "🔢",
      "label": "value.seq",
      "manager": "@c15r/lit",
      "note": "Ordering decoration. Key `_doc/<docId>/<blockKey>`; the key-encoded rule (ADR-0003) projects it into a `block inDoc doc:<docId>` Reference edge — doc membership as graph.",
      "schema": {
        "seq": "number — narrative position (fractional, e.g. 2.25, allows insertion)",
        "fold": "boolean? — collapsed by default"
      },
      "keyPattern": "_doc/{doc}/{block}",
      "keyEdges": [
        {
          "from": "{block}",
          "rel": "inDoc",
          "to": "doc:{doc}"
        }
      ]
    }
  ]
}
