/** Session = the kernel's (sign in once, origin-wide; apex-aware). Reference, not copy. */
export { ensureAuth, isAuthed, accessToken, authFetch, login, signOut, requestScopes } from 'https://parc.land/@c15r/kernel/app.js';
