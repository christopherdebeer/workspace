{
  "name": "home-devtools",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "serve": "node serve.mjs",
    "dev": "node build.mjs && (node build.mjs --watch & node serve.mjs)"
  },
  "devDependencies": {
    "esbuild": "^0.25.5"
  }
}
